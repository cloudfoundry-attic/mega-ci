#!/bin/sh

set -eu

fly \
  -t mega \
  sp \
  -p etcd \
  -c ./pipelines/etcd.yml \
  -v mega-ci-env-private-key="$(cat /Users/pivotal/workspace/mega-ci-env/deploy_keys/cloudfoundry/mega-ci-env)" \
  -v etcd-release-private-key="$(cat /Users/pivotal/workspace/mega-ci-env/deploy_keys/cloudfoundry-incubator/etcd-release)" \
  -l /Users/pivotal/workspace/mega-ci-env/pipeline_vars/etcd.yml
  # -v docker-username="cloudfoundrydevelopers" \
  # -v docker-password="scream8badger" \
  # -v docker-email="cf-toolsmiths+dockeross@pivotal.io"
