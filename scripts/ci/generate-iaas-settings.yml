---
platform: linux

image: docker:///cfinfrastructure/deployment

inputs:
  - name: mega-ci
  - name: private-credentials

run:
  path: mega-ci/scripts/ci/generate-iaas-settings

params:
  DEPLOYMENT_DIR:
  DEPLOYMENT:
  SECURITY_GROUP:
  STACK_NAME:
  SUBNET:
