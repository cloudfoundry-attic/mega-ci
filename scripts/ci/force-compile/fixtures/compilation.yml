---
director_uuid: REPLACE_ME_DIRECTOR_UUID

name: etcd-compilation

releases:
- name: consul
  version: REPLACE_ME_CONSUL_VERSION
- name: turbulence
  version: REPLACE_ME_TURBULENCE_VERSION
- name: bosh-aws-cpi
  version: REPLACE_ME_BOSHAWSCPI_VERSION

update:
  canaries: 1
  max_in_flight: 1
  serial: false
  canary_watch_time: 1000-60000
  update_watch_time: 1000-60000

stemcells:
- alias: trusty
  os: ubuntu-trusty
  version: latest

instance_groups: []
