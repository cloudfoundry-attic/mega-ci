---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: mega-ci
  - name: release-repo
  - name: release-repo-master
  - name: cf-release
  - name: consul-release
  - name: etcd-release
  - name: stemcell

outputs:
  - name: blessed-repo

params:
  BLESSED_REPO: blessed-repo

run:
  path: mega-ci/scripts/ci/commit-blessing/task
