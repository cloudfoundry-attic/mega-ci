---
platform: linux

image: docker:///cfinfrastructure/mega-ci

inputs:
  - name: mega-ci
  - name: private-credentials
  - name: release-repo
  - name: release-repo-master

outputs:
  - name: final-release-repo

run:
  path: mega-ci/scripts/ci/create-final-release/task

params:
  RELEASE_NAME:
  MASTER_BRANCH:
