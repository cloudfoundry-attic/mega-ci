---
platform: linux

image: docker:///cfinfrastructure/deployment

inputs:
  - name: mega-ci
  - name: private-credentials

run:
  path: mega-ci/scripts/ci/generate-property-overrides-turbulence

params:
  DEPLOYMENT_DIR:
