---
director_uuid: REPLACE_ME_DIRECTOR_UUID

name: etcd-compilation

releases:
- name: etcd
  version: REPLACE_ME_ETCD_VERSION

update:
  canaries: 1
  max_in_flight: 1
  serial: false
  canary_watch_time: 1000-60000
  update_watch_time: 1000-60000

stemcells:
- alias: trusty
  os: ubuntu-trusty
  version: latest

instance_groups: []
