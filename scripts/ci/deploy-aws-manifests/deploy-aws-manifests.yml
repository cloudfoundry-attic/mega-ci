---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: mega-ci
  - name: release

params:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:

run:
  path: mega-ci/scripts/ci/deploy-aws-manifests/deploy-aws-manifests

