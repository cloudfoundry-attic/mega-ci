---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: mega-ci
  - name: etcd-release
  - name: cf-release
  - name: cf-deployment
  - name: deployments-runtime

run:
  path: mega-ci/scripts/ci/deploy-cf

params:
  STUB_PATH:
  STUB_PARTS_PATH:
