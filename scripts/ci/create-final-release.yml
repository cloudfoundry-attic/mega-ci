---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: mega-ci
  - name: private-credentials
  - name: release-repo

run:
  path: mega-ci/scripts/ci/create-final-release

params:
  RELEASE_NAME:
  PRIVATE_YML_PATH:
